name: cache-warm

# Manually triggered: builds the dev shell on each supported nix system and
# pushes the resulting closure to rood-reverse.cachix.org so contributors don't
# pay the from-source cross-gcc cost on first `nix develop`.
#
# Run this once per nixpkgs bump (or whenever flake.lock changes), not on every
# push — it's billed minutes for something contributors only need occasionally.
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - flake.nix
      - flake.lock

jobs:
  cache-warm:
    strategy:
      fail-fast: false
      matrix:
        include:
          - system: x86_64-linux
            runner: ubuntu-latest
          - system: aarch64-darwin
            runner: macos-latest
    name: cache-warm (${{ matrix.system }})
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          extra_nix_config: |
            accept-flake-config = true
            experimental-features = nix-command flakes

      - name: Push to rood-reverse cachix
        uses: cachix/cachix-action@v15
        with:
          name: rood-reverse
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}

      - name: Build dev shell
        run: nix build --print-out-paths .#devShells.${{ matrix.system }}.default
