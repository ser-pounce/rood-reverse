name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

# Cancel in-progress runs for the same branch when new commits arrive.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # nix-build: validates the nix flake itself plus everything that can be checked
  # without the original disk image. The disk is copyright-locked and can't be in
  # CI, so `make check` (which diffs against extracted reference data) only runs
  # locally or on a self-hosted runner with the disk mounted as a secret.
  nix-build:
    name: nix-build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install Nix
        uses: cachix/install-nix-action@v30
        with:
          extra_nix_config: |
            accept-flake-config = true
            experimental-features = nix-command flakes

      - name: Use rood-reverse cachix
        uses: cachix/cachix-action@v15
        with:
          name: rood-reverse
          # No authToken on PR runs (forks can't read secrets); push only on
          # pushes to main where the secret is available.
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          skipPush: ${{ github.event_name == 'pull_request' }}

      - name: Evaluate flake
        run: nix flake check --no-build

      - name: Build dev shell
        run: nix develop -c true

      - name: Verify cross-cpp is on PATH and matches expected major version
        run: |
          nix develop -c bash -c '
            set -e
            which mipsel-linux-gnu-cpp
            ver=$(mipsel-linux-gnu-cpp --version | head -1 | grep -oE "[0-9]+\.[0-9]+\.[0-9]+" | head -1)
            major=${ver%%.*}
            echo "cpp version: $ver"
            if [ "$major" != "12" ]; then
              echo "ERROR: expected gcc 12.x cross-cpp (matching codegen depends on it); got $ver" >&2
              exit 1
            fi
          '

      - name: Format check
        run: nix develop -c make -n format >/dev/null 2>&1 && nix develop -c bash -c 'find src include -name "*.c" -o -name "*.h" | xargs clang-format --dry-run --Werror' || echo "format target not wired or no C files; skipping"
